<Controls:MetroWindow x:Class="VpdbAgent.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VpdbAgent"
        xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
		xmlns:UserControls="clr-namespace:VpdbAgent.Pages"
        mc:Ignorable="d"
		Title="{Binding Path=Content.Title, ElementName=MainFrame}"
		Height="800" Width="600">
    <DockPanel LastChildFill="True">

        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal">
            <Label Content="Status info. All green." />
            <Button  x:Name="SettingsButton" Content="Settings" Width="75" Click="SettingsButton_Click" />
        </StackPanel>

        <Frame x:Name="MainFrame">
            <Frame.Template>
                <ControlTemplate TargetType="Frame">
                    <DockPanel Margin="7" >
                        <StackPanel Margin="7" Orientation="Horizontal" DockPanel.Dock="Top">
                            <Button Command="{x:Static NavigationCommands.BrowseBack}" IsEnabled="{TemplateBinding CanGoBack}" Margin="0,0,10,0" >
                                <Canvas Width="42.765" Height="73.997">
                                    <Canvas>
                                        <Path Fill="{Binding Foreground, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Button}}" Data="F1 M 11.621,37.042 L 42.682,5.810 L 36.872,0.000 L 0.000,37.042 L 36.954,73.997 L 42.765,68.186 L 11.621,37.042 Z"/>
                                    </Canvas>
                                </Canvas>
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Foreground" Value="Red" />
                                            </Trigger>
                                            <Trigger Property="IsMouseOver" Value="False">
                                                <Setter Property="Foreground" Value="Gray" />
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                            <Button Command="{x:Static NavigationCommands.BrowseForward}" IsEnabled="{TemplateBinding CanGoForward}">
                                <Viewbox Width="15" Height="15">
                                    <Frame Source="Resources/Icons/angle-right.xaml" />
                                </Viewbox>
                            </Button>
                        </StackPanel>
                        <ContentPresenter />
                    </DockPanel>
                </ControlTemplate>
            </Frame.Template>
        </Frame>

    </DockPanel>
</Controls:MetroWindow>
