<Controls:MetroWindow x:Class="VpdbAgent.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VpdbAgent"
        xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
		xmlns:UserControls="clr-namespace:VpdbAgent.Views"
        mc:Ignorable="d"
        Title="VPDB Agent" Height="800" Width="600">
    <DockPanel LastChildFill="True">
        <Button Content="Dock=Left" DockPanel.Dock="Bottom"/>
        <Grid>
           
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button Grid.Row="0" Grid.Column="0" x:Name="SettingsButton" Content="Settings" HorizontalAlignment="Center" VerticalAlignment="Top" Width="75" Click="SettingsButton_Click" RenderTransformOrigin="4.624,5.858"/>

			
            <GridSplitter HorizontalAlignment="Right" 
                  VerticalAlignment="Stretch" 
                  Grid.Column="1" ResizeBehavior="PreviousAndNext"

                  Width="5" Background="#FFBCBCBC"/>
            <Grid Grid.Row="0" Grid.Column="2" Background="#111">
                <ItemsControl Name="ReleaseList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <UserControls:ReleaseTemplate Release="{Binding}" Margin="4" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>

                </ItemsControl>
            </Grid>

        </Grid>

    </DockPanel>
   
</Controls:MetroWindow>
